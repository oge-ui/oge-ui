// The card vocabulary lives framework-free in `@oge-ui/behavior`
// (`layout-core`), shared with the React render layer; re-exported here so
// existing imports keep working.
export type {
  OgeCardStylingMode,
  OgeCardOrientation,
  OgeCardSize,
  OgeCardSeverity,
  OgeCardActionsAlign,
} from '@oge-ui/behavior';
