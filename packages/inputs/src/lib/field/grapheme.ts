// Moved to `@oge-ui/behavior` (ADR 0001 — the React text editors count the
// same way); re-exported so package-internal import paths stay unchanged.
export { graphemeCount, resetGraphemeSegmenter } from '@oge-ui/behavior';
