// The color box vocabulary and the default palette live framework-free in
// `@oge-ui/behavior` (`color-core`), shared with the React render layer;
// re-exported here so existing imports keep working.
export {
  OGE_DEFAULT_COLOR_PALETTE,
  type OgeColorBoxApplyValueMode,
  type OgeColorBoxView,
} from '@oge-ui/behavior';
