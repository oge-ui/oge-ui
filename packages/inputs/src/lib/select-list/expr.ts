// Moved to `@oge-ui/behavior` (ADR 0001 — the React select-shaped controls
// resolve the same way); re-exported so package-internal paths stay unchanged.
export {
  resolveDisplay,
  resolveValue,
  resolveDisabled,
} from '@oge-ui/behavior';
